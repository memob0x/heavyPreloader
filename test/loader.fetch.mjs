describe("fetch function", () => {
    it("should return a promise which resolves to a resource blob object", (done) =>
        done());

    it("should return a cached promise when a resource has already been fetched", (done) =>
        done());
});
