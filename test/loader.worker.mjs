describe("worker", () => {
    it("should be able to create a worker dynamically", (done) => done());

    it("should be able to retrieve a previously created worker without creating a new one", (done) =>
        done());

    it("should be able to terminate the worker when all instances are disposed", (done) =>
        done());
});
