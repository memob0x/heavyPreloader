describe("html loader", () => {
    it("should return a promise which resolves to plain text", (done) =>
        done());

    it("should return a promise which resolves to plain text", (done) =>
        done());

    it("should insert retrieved html to an element if provided in options", (done) =>
        done());

    it("should filter retrieved html if filter is provided in options", (done) =>
        done());
});
