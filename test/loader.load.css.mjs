describe("stylesheet loader", () => {
    it("should return a promise which resolves to a CSSStyleSheet object", (done) =>
        done());

    it("should attach stylesheet to document if no different option is specified", (done) =>
        done());
});
