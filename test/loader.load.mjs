describe("load function", () => {
    it("should be able to recognize image media type blob objects", (done) =>
        done());

    it("should be able to recognize css media type blob objects", (done) =>
        done());

    it("should be able to recognize javascript media type blob objects", (done) =>
        done());

    it("should be able to recognize html media type blob objects", (done) =>
        done());
});
