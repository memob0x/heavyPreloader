describe("image loader", () => {
    it("should return a promise which resolves to a load/error Event", (done) =>
        done());

    it("should attach resource to a non existent image element if no other image element is provied in options", (done) =>
        done());
});
