import {
    loadImage,
    loadStyle,
    loadObject,
    loadScript
} from "../src/loader.loaders.mjs";
import Loaders from "../src/loader.loaders.mjs";

describe("loaders", () => {
    it("should test loaders", done => done());
});
